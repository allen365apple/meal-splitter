# Legacy single-file version / 單檔版本

This is the original all-in-one `index.html` — React and Tailwind loaded from CDN and JSX
compiled in the browser. Kept for reference. It works by just opening the file, but the
deployed site is built from `../src/` instead (see the root README).

這是最初的單檔版本，React 與 Tailwind 從 CDN 載入、JSX 在瀏覽器即時編譯。
保留作參考。直接開檔就能用，但正式部署的版本是從 `../src/` 建置的（見根目錄 README）。
